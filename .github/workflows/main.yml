name: CI

env:
  REGISTRY: ghcr.io
  REGISTRY_IMAGE: ${{ github.repository }}

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Download dependencies
        run: go mod download

      - name: Run tests
        run: go test -v ./...

      - name: Build
        run: go build -o api-server ./cmd/api

  docker-build:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log into GitHub Container Registry
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY}}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Get Docker Metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/joejn/cd-mcm-exercise-neuhauser/product-catalog

      - name: Build Docker Container
        id: push
        uses: docker/build-push-action@v6
        with:
          context: .
          tags: |
            ghcr.io/joejn/cd-mcm-exercise-neuhauser/product-catalog:${{ github.sha }}
            ghcr.io/joejn/cd-mcm-exercise-neuhauser/product-catalog:latest
          # push: true
          push: ${{ github.ref == 'refs/heads/main' }}
